
// check is leap year or not 

function isLeapYear(year) {

    // const isLeap = year / 4;
    if (year % 4 === 0) {
        return true;
    } else {
        return false;
    }
}
const isLeap = isLeapYear(1662);
// console.log(isLeap);





